const { topMoversRouter } = require('./topMovers.route');
const { healthRouter } = require('./health.route');
const { buyStocksRouter } = require('./buyStocks.route');

module.exports = { topMoversRouter, healthRouter, buyStocksRouter };
